[user]
  name = Owen Davies
  email = obduk@users.noreply.github.com
[color]
  ui = auto
[push]
  default = current
[alias]
  info = ! git branch && git status -s && git stash list
  sync = ! git pull && git remote prune origin
[core]
  excludesfile = ~/.gitignore
